/*
import { verifyBrew } from "../src/brewVerifier";
import { expect, test } from "vitest";

test("basic brew test", () => {
  const result = verifyBrew("seed", [], { targetScore: 100, maxMoves: 5, optimalMoves: 3 });
  expect(result).toBeGreaterThanOrEqual(0);
});
*/
