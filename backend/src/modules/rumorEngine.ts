import { GameState, Rumor } from '../../shared/types';

/**
 * Generate new rumors based on game state.
 * For now, returns an empty array (stub).
 */
export function generateRumors(state: GameState): Rumor[] {
  // TODO: Analyze state.market, quests, and black market to produce rumors
  return [];
}
