{
  "name": "coven-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --filter ./backend dev & pnpm --filter ./frontend dev",
    "test": "pnpm -r test",
    "lint": "eslint ."
  },
  "devDependencies": {
    "vitest": "^1.5.0"
  },
  "dependencies": {
    "seedrandom": "^3.0.5"
  }
}
