{
  "name": "coven-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --filter ./backend dev & pnpm --filter ./frontend dev",
    "test": "pnpm -r test",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@types/uuid": "^10.0.0",
    "vitest": "^1.5.0"
  },
  "dependencies": {
    "seedrandom": "^3.0.5",
    "uuid": "^11.1.0"
  }
}
